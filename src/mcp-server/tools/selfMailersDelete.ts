/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { selfMailersDelete } from "../../funcs/selfMailersDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SelfMailersDeleteRequest$inboundSchema,
};

export const tool$selfMailersDelete: ToolDefinition<typeof args> = {
  name: "self-mailers-delete",
  description: `Cancel Self Mailer

Cancel a self-mailer by ID. Note that this operation cannot be undone.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await selfMailersDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
