/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postcardsGet } from "../../funcs/postcardsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostcardsGetRequest$inboundSchema,
};

export const tool$postcardsGet: ToolDefinition<typeof args> = {
  name: "postcards-get",
  description: `Get Postcard

Retrieve a postcard by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await postcardsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
