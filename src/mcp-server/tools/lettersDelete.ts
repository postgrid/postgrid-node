/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lettersDelete } from "../../funcs/lettersDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.LettersDeleteRequest$inboundSchema,
};

export const tool$lettersDelete: ToolDefinition<typeof args> = {
  name: "letters-delete",
  description: `Cancel Letter

Cancel a letter by ID. Note that this operation cannot be undone.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lettersDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
