/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { chequesList } from "../../funcs/chequesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ChequesListRequest$inboundSchema,
};

export const tool$chequesList: ToolDefinition<typeof args> = {
  name: "cheques-list",
  description: `List Cheques

Get a list of cheques.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await chequesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
