/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bankAccountsDelete } from "../../funcs/bankAccountsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.BankAccountsDeleteRequest$inboundSchema,
};

export const tool$bankAccountsDelete: ToolDefinition<typeof args> = {
  name: "bank-accounts-delete",
  description: `Delete Bank Account

Delete a bank account by ID. Note that this operation cannot be undone.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bankAccountsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
