/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { chequesGetDepositReadyPdf } from "../../funcs/chequesGetDepositReadyPdf.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ChequesGetDepositReadyPDFRequest$inboundSchema,
};

export const tool$chequesGetDepositReadyPdf: ToolDefinition<typeof args> = {
  name: "cheques-get-deposit-ready-pdf",
  description: `Retrieve Cheque Deposit-Ready PDF (Digital Only)

Retrieve the deposit-ready PDF for a digital-only cheque. The endpoint can only be called by users with 'Admin' role.
In test mode, the preview PDF of the digitalOnly cheque and the deposit-ready PDF are the same.
In live mode, the deposit-ready will have the full account number.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await chequesGetDepositReadyPdf(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
