/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { chequesGet } from "../../funcs/chequesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ChequesGetRequest$inboundSchema,
};

export const tool$chequesGet: ToolDefinition<typeof args> = {
  name: "cheques-get",
  description: `Get Cheque

Retrieve a cheque by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await chequesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
