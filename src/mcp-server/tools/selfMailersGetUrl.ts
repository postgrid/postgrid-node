/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { selfMailersGetUrl } from "../../funcs/selfMailersGetUrl.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SelfMailersGetURLRequest$inboundSchema,
};

export const tool$selfMailersGetUrl: ToolDefinition<typeof args> = {
  name: "self-mailers-get-url",
  description: `Get Self Mailer Preview

Retrieve a self-mailer preview URL.

This is only available for customers with our document management addon, which offers
document generation and hosting capabilities. This endpoint has a much higher rate limit
than the regular order retrieval endpoint, so it is suitable for customer-facing use-cases.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await selfMailersGetUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
